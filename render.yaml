services:
  - type: web
    name: mpesa-payment-gateway-demo
    runtime: static
    buildCommand: echo "Using committed frontend build"
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
